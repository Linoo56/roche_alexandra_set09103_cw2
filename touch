class School:

        def __init__(self, userEmail, score, content, status, checkedBy, checkedDate, schid):
                self.userEmail = userEmail
                self.score = score
                self.content = content
		self.status = status
                self.checkedBy = checkedBy
                self.checkedDate = checkedDate
                self.schid = schid

