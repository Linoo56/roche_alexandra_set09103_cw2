class Review:
	
	def __init__(self, userEmail, score, content, status, checkedBy, checkingDate, schid):
		self.userEmail = userEmail
		self.score = score
		self.content = content
		self.status = status
		self.checkedBy = checkedBy
		self.checkingDate = checkingDate
		self.schid = schid

